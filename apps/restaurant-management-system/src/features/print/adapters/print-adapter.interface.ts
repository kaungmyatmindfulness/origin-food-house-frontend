/**
 * Print adapter interface and types.
 * Re-exports from types for convenience when implementing adapters.
 */

export type {
  PrintAdapter,
  PrintJob,
  PrintPlatform,
  PrintResult,
} from '../types/print.types';
