// breadcrumbs <empty space> create menu item button (with icon), create category button (with icon)
// on click crete menu item button should open a modal with a form to create a new menu item
// on click create category button should open a modal with a form to create a new category

// menu items and categories section
// menu item will be displayed with a card component
// each menu item will have a delete and edit button in a popover
// card component will have a title, description, price, and an image
// clicking on the card will open a modal with the menu item details
// menu items will be grouped by categories
// categories be displayed as accordion items
// category header will have a title, delete and edit button

export default function MenuPage() {
  return <div>{/* Menu content goes here */}</div>;
}
