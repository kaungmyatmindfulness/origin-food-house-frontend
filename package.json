{
  "name": "origin-food-house",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:api": "turbo run dev --filter=@app/api",
    "dev:rms": "turbo run dev --filter=@app/restaurant-management-system",
    "dev:sos": "turbo run dev --filter=@app/self-ordering-system",
    "dev:admin": "turbo run dev --filter=@app/admin-platform",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "test": "turbo run test",
    "test:cov": "turbo run test:cov",
    "generate:api": "npm run generate --workspace=@repo/api",
    "db:generate": "turbo run db:generate --filter=@app/api",
    "db:migrate": "turbo run db:migrate --filter=@app/api",
    "db:seed": "turbo run db:seed --filter=@app/api",
    "db:studio": "turbo run db:studio --filter=@app/api",
    "tauri:dev": "npm run tauri:dev --workspace=@app/restaurant-management-system",
    "tauri:build": "npm run tauri:build --workspace=@app/restaurant-management-system",
    "tauri:build:debug": "npm run tauri:build:debug --workspace=@app/restaurant-management-system",
    "tauri:android:init": "npm run tauri:android:init --workspace=@app/restaurant-management-system",
    "tauri:android:dev": "npm run tauri:android:dev --workspace=@app/restaurant-management-system",
    "tauri:android:build": "npm run tauri:android:build --workspace=@app/restaurant-management-system",
    "tauri:ios:init": "npm run tauri:ios:init --workspace=@app/restaurant-management-system",
    "tauri:ios:dev": "npm run tauri:ios:dev --workspace=@app/restaurant-management-system",
    "tauri:ios:build": "npm run tauri:ios:build --workspace=@app/restaurant-management-system"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-tailwindcss": "^0.7.1",
    "shadcn": "^3.5.0",
    "turbo": "^2.6.1",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@11.6.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@app/admin-platform": "^0.1.0",
    "@app/api": "^0.0.1",
    "@app/restaurant-management-system": "^0.1.0",
    "@app/self-ordering-system": "^0.1.0"
  }
}
